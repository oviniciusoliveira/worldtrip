export type ContinentResponse = {
  title: string;
  subtitle?: string;
  imageFile: string;
  slug: string;
  description: string;
  countriesCount: number;
  languagesCount: number;
  citiesCount: number;
};

export type Continent = {
  title: string;
  subtitle?: string;
  file: string;
  alt: string;
  slug: string;
  description: string;
  countriesCount: number;
  languagesCount: number;
  citiesCount: number;
};
